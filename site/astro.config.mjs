// @ts-check
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import sidebar from './src/data/sidebar.json' with { type: 'json' };

// Phase 2 builds under /v2/ until the Phase 3 cutover.
// The sidebar is generated by scripts/compare/site-data.mjs - run it before building.
export default defineConfig({
	base: process.env.SITE_BASE || '/v2',
	integrations: [
		starlight({
			title: 'Sifututor Design System',
			customCss: ['./src/styles/sifu.css'],
			components: {
				ThemeSelect: './src/components/NoThemeSelect.astro',
				ThemeProvider: './src/components/ForceLight.astro',
			},
			sidebar,
		}),
	],
});

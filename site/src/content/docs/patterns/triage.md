---
title: Triage feeds
description: Rich complete rows in fixed scan zones.
---

Triage surfaces (the requests feed) use **rich, complete rows** with every fact in a **fixed scan zone**, so the eye learns one geometry. Rows beat cards; swipe-to-apply class interactions belong to marketplace-grounded triage. Hard fit filters (like gender preference) are never dropped from the row.
